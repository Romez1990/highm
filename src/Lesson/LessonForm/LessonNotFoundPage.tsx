import React from 'react';
import { ErrorPage } from '../../ErrorPage';

function LessonNotFoundPage(): JSX.Element {
  return <ErrorPage title="Практическая работа не найдена" />;
}

export default LessonNotFoundPage;
